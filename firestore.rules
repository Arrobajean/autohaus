rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas públicas: lectura y escritura permitidas para todos
    // Útil para desarrollo y aplicaciones de uso interno
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

